Pretty low-level utils. Allows various ways to concat files.

    concat BYTE : simply concat files in one

    concat TEXT : concat and add new line return between each files

    concat JSIIFE : concat in a JS Immediately Invoked Function Expression

    concat INC : concat and inject in a "template" (existing file as model)
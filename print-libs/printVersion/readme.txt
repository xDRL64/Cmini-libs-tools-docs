Description :

	A function to print the num of app's version, if called with args :

		-v
	
		--v

		-version

		--version

Using :

	Needs : regex.h

	Uses a const nammed VERSION (string)

	Returns 1 if version has been printed or if internal error

	(else return 0)